(()=>{
'use strict';
if(window.__ANDES_LAB_UX_V7__)return;
window.__ANDES_LAB_UX_V7__=true;
const PRIMARY={1:'s1-diagnostico',2:'sql-s2',3:'sql-s3',4:'sql-s4',5:'sql-s5',6:'s6-reglas-evidencia',7:'erd-s7',8:'erd-s8',9:'s9-constraints',10:'decision-s10',11:'s11-documentos',12:'warehouse-s12',13:'bigquery-s13',14:'unnest-s14',15:'s15-integrador',16:'s16-dp900'};
const AUTH='andesdb.lms.auth.v1',PREFIX='andesdb.lab.answers.v2.',remoteCompleted=new Set();
const session=Number(new URLSearchParams(location.search).get('session'))||0;
const codeFor=i=>i===10?PRIMARY[session]:`s${session}-r${i}`;
const safeJSON=(s,f)=>{try{return JSON.parse(s)}catch{return f}};
const owner=()=>{const a=safeJSON(localStorage.getItem(AUTH)||'null',null);return String(a?.user?.id||a?.user?.username||'anonymous').replace(/[^a-zA-Z0-9_-]/g,'_')};
const key=()=>`${PREFIX}${owner()}.s${session}`;
const practice=()=>{const m=document.getElementById('task-num')?.textContent?.match(/Práctica\s+(\d+)/i);return m?Number(m[1]):Number(document.querySelector('.steps .current')?.dataset?.step||1)};
const trim=v=>String(v??'').slice(0,6000);
let store=null,storeDirty=false,writeTimer=null,idleHandle=null,restoring=false;
const getStore=()=>store||(store=safeJSON(localStorage.getItem(key())||'{}',{})||{});
function persistNow(){if(!storeDirty||!store)return;try{localStorage.setItem(key(),JSON.stringify(store));storeDirty=false}catch(_){}}
function schedulePersist(delay=700){
  clearTimeout(writeTimer);if(idleHandle&&window.cancelIdleCallback)cancelIdleCallback(idleHandle);idleHandle=null;
  writeTimer=setTimeout(()=>{writeTimer=null;if(window.requestIdleCallback)idleHandle=requestIdleCallback(()=>{idleHandle=null;persistNow()},{timeout:700});else persistNow()},delay);
}
function snapshot(i=practice()){
  const task=window.ANDES_LAB_CONTENT?.sessions?.[session]?.tasks?.[i-1];if(!task)return null;
  if(task.type==='sql')return {kind:'sql',value:trim(document.getElementById('sql-editor')?.value||'')};
  if(task.type==='classify')return {kind:'classify',values:[...document.querySelectorAll('[data-classify]')].map(x=>trim(x.value))};
  if(task.type==='order')return {kind:'order',values:[...document.querySelectorAll('.order-row span')].map(x=>trim(x.textContent))};
  return {kind:'text',value:trim(document.getElementById('text-answer')?.value||'')};
}
function put(i,slot,snap,{immediate=false}={}){if(!session||!i||!snap)return;const all=getStore(),e=all[i]||{};const next=JSON.stringify(snap),prev=e[slot]?JSON.stringify(e[slot]):'';if(next===prev)return;e[slot]=snap;e.updated_at=new Date().toISOString();all[i]=e;storeDirty=true;if(immediate)persistNow();else schedulePersist()}
const saveDraft=(immediate=false)=>{const i=practice(),snap=snapshot(i);if(snap)put(i,'draft',snap,{immediate})};
const saveCorrect=()=>{const i=practice(),snap=snapshot(i);if(snap){put(i,'correct',snap);put(i,'draft',snap,{immediate:true})}return snap};
function preferred(i,api){const e=getStore()[i],done=Boolean(api?.localCompleted?.(codeFor(i))||remoteCompleted.has(codeFor(i)));if(e)return (done&&e.correct)||e.draft||e.correct||null;if(done){const task=window.ANDES_LAB_CONTENT?.sessions?.[session]?.tasks?.[i-1];if(task?.type==='classify'&&Array.isArray(task.answers))return {kind:'classify',values:[...task.answers]};if(task?.type==='order'&&Array.isArray(task.answer))return {kind:'order',values:[...task.answer]}}return null}
function fire(el,type){try{el.dispatchEvent(new Event(type,{bubbles:true}))}catch(_){}}
const tick=()=>new Promise(r=>setTimeout(r,0));
async function restoreOrder(saved){if(!Array.isArray(saved?.values)||!saved.values.length)return;for(let target=0;target<saved.values.length;target++){let guard=0;while(guard++<20){const rows=[...document.querySelectorAll('.order-row')];if(!rows.length)return;const current=rows.map(r=>r.querySelector('span')?.textContent||'');const at=current.indexOf(saved.values[target]);if(at<0||at===target)break;if(at>target){rows[at].querySelector('[data-up]')?.click();await tick()}else{rows[at].querySelector('[data-down]')?.click();await tick()}}}}
function ensureStyle(){if(document.getElementById('andes-lab-ux7-style'))return;const s=document.createElement('style');s.id='andes-lab-ux7-style';s.textContent=`.mcq-row{display:block!important}.mcq-prompt{display:block;font-weight:850;margin-bottom:9px}.mcq-native{position:absolute!important;opacity:0!important;pointer-events:none!important;width:1px!important;height:1px!important}.mcq-options{display:grid;gap:8px}.mcq-option{display:grid;grid-template-columns:22px minmax(0,1fr);gap:9px;align-items:start;border:1px solid #d9e0e8;border-radius:10px;padding:10px 11px;background:#fff;cursor:pointer}.mcq-option:has(input:checked){border-color:#175cd3;box-shadow:0 0 0 3px #dbe7ff;background:#f8fbff}.mcq-option input{margin-top:3px;width:17px;height:17px}.answer-saved{margin-top:8px;font-size:.72rem;color:#166534;font-weight:750}@media(max-width:820px){body.lab-editing .mobile-nav{display:none!important}body.lab-editing main{padding-bottom:24px!important}.sql-editor,.text-answer{scroll-margin-bottom:28vh}}`;document.head.appendChild(s)}
function enhanceMCQ(i){const task=window.ANDES_LAB_CONTENT?.sessions?.[session]?.tasks?.[i-1];if(!task?.mcq)return;const select=document.querySelector('[data-classify="0"]');if(!select||select.dataset.mcqEnhanced==='1')return;ensureStyle();select.dataset.mcqEnhanced='1';select.classList.add('mcq-native');const old=select.closest('.classify-row');if(!old)return;const row=document.createElement('div');row.className='classify-row mcq-row';const prompt=document.createElement('span');prompt.className='mcq-prompt';prompt.textContent=old.querySelector('span')?.textContent||'Selecciona una opción';row.appendChild(prompt);row.appendChild(select);const opts=document.createElement('div');opts.className='mcq-options';opts.setAttribute('role','radiogroup');[...select.options].filter(o=>o.value).forEach(o=>{const lab=document.createElement('label');lab.className='mcq-option';const radio=document.createElement('input');radio.type='radio';radio.name=`mcq-s${session}-p${i}`;radio.value=o.value;radio.checked=select.value===o.value;radio.addEventListener('change',()=>{if(radio.checked){select.value=radio.value;fire(select,'change')}});const text=document.createElement('span');text.textContent=o.textContent;lab.append(radio,text);opts.appendChild(lab)});row.appendChild(opts);old.replaceWith(row)}
function anchor(){return document.querySelector('#sql-editor,#text-answer,[data-classify],.order-list')}
async function restore(api){if(restoring)return;const i=practice(),task=window.ANDES_LAB_CONTENT?.sessions?.[session]?.tasks?.[i-1],a=anchor();if(!task||!a)return;if(a.dataset.restoredV7==='1'){enhanceMCQ(i);return}restoring=true;try{const saved=preferred(i,api);if(saved){if(task.type==='sql'){const e=document.getElementById('sql-editor');if(e&&saved.kind==='sql'){e.value=saved.value||'';fire(e,'input')}}else if(task.type==='classify'&&saved.kind==='classify'){[...document.querySelectorAll('[data-classify]')].forEach((e,k)=>{if(saved.values?.[k]!=null){e.value=saved.values[k];fire(e,'change')}})}else if(task.type==='order'&&saved.kind==='order')await restoreOrder(saved);else{const e=document.getElementById('text-answer');if(e&&saved.kind==='text'){e.value=saved.value||'';fire(e,'input')}}}enhanceMCQ(i);const now=anchor();if(now)now.dataset.restoredV7='1'}finally{restoring=false}}
function installPersistence(api){
  ensureStyle();
  document.addEventListener('input',e=>{if(restoring)return;if(e.target?.matches?.('#sql-editor,#text-answer'))scheduleDraft()},true);
  document.addEventListener('change',e=>{if(restoring)return;if(e.target?.matches?.('[data-classify]')){saveDraft();schedulePersist(250)}},true);
  document.addEventListener('click',e=>{if(e.target?.closest?.('[data-up],[data-down]'))setTimeout(()=>{saveDraft();schedulePersist(200)},40)},true);
  document.addEventListener('focusin',e=>{if(e.target?.matches?.('#sql-editor,#text-answer'))document.body.classList.add('lab-editing')},true);
  document.addEventListener('focusout',e=>{if(e.target?.matches?.('#sql-editor,#text-answer'))setTimeout(()=>{if(!document.activeElement?.matches?.('#sql-editor,#text-answer')){document.body.classList.remove('lab-editing');saveDraft(true)}},60)},true);
  addEventListener('pagehide',()=>persistNow());
  document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='hidden')persistNow()});
  const root=document.getElementById('task');if(root)new MutationObserver(()=>queueMicrotask(()=>restore(api))).observe(root,{childList:true});
  setTimeout(()=>restore(api),60);
}
let draftTimer=null;function scheduleDraft(){clearTimeout(draftTimer);draftTimer=setTimeout(()=>{draftTimer=null;saveDraft(false)},650)}
function installFastProgress(api){if(api.__labFastProgressV7||typeof api.dashboard!=='function')return;api.__labFastProgressV7=true;const original=api.dashboard.bind(api),endpoint='https://gnpouhsvsisqoxketlfr.supabase.co/functions/v1/learning-progress';let inflight=null,last=null,lastAt=0;api.dashboard=function(scope='me',fresh=false){if(scope!=='me'||fresh!==true||!/\/lab\.html$/i.test(location.pathname))return original(scope,fresh);if(last&&Date.now()-lastAt<1000)return Promise.resolve(last);if(inflight)return inflight;inflight=(async()=>{const a=safeJSON(localStorage.getItem(AUTH)||'null',null);if(!a?.token)return original(scope,fresh);const absorb=d=>{remoteCompleted.clear();for(const x of(d?.activity_progress||[]))if(x.status==='completed')remoteCompleted.add(String(x.activity_code));setTimeout(()=>restore(api),0);return d};const c=new AbortController(),timer=setTimeout(()=>c.abort(),8000);try{const r=await fetch(endpoint,{method:'GET',headers:{Authorization:'Bearer '+a.token},signal:c.signal,cache:'no-store'});if(!r.ok)throw new Error(`HTTP ${r.status}`);last=absorb(await r.json());lastAt=Date.now();return last}catch(e){console.warn('ANDESDB: progreso ligero no disponible; uso respaldo completo',e);return absorb(await original(scope,fresh))}finally{clearTimeout(timer);inflight=null}})();return inflight}}
function installOptimisticComplete(api){if(api.__labOptimisticV7||typeof api.complete!=='function')return;api.__labOptimisticV7=true;const original=api.complete.bind(api);api.complete=function(activity,score=1,meta={},s=null){const i=practice(),answer=saveCorrect(),enriched={...meta,answer:answer||undefined,answer_saved:true,ux:'lab-v7'};let p;try{p=original(activity,score,enriched,s)}catch(e){console.warn('ANDESDB: no se pudo iniciar sincronización',e);return Promise.resolve(false)}Promise.resolve(p).then(()=>window.dispatchEvent(new CustomEvent('andesdb:answer-synced',{detail:{session,practice:i,activity}}))).catch(e=>console.warn('ANDESDB: sincronización diferida',e));return Promise.resolve(true)}}
window.__ANDES_LAB_PATCH_READY__=(async()=>{const started=Date.now();while(!window.ANDES_LMS&&Date.now()-started<12000)await new Promise(r=>setTimeout(r,30));const api=window.ANDES_LMS;if(!api)return false;getStore();installFastProgress(api);installOptimisticComplete(api);installPersistence(api);return true})();
})();