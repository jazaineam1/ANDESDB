window.ANDESDB_S3_STEPS=[
{id:'s3-start',unit:'s3',kind:'learn',title:'De filas a respuestas',short:'El cambio de mentalidad',minutes:4,xp:10,skills:['WHERE','GROUP BY'],lead:'No empiezas de cero: gran parte de lo que ya haces con filtros y tablas dinámicas en Excel tiene una traducción directa a SQL.',body:'<p class="explain">Hasta la sesión anterior SQL servía sobre todo para <strong>listar filas</strong>. Ahora lo usamos para responder preguntas: filtrar con precisión, resumir miles de registros y decidir el nivel de detalle.</p><div class="analogy"><div class="box"><b>En Excel</b><br>Filtro · “entre” · casillas · tabla dinámica</div><div class="arrow">→</div><div class="box"><b>En SQL</b><br><code>WHERE</code> · <code>BETWEEN</code> · <code>IN</code> · <code>GROUP BY</code></div></div><div class="callout"><strong>Idea guía.</strong> Una consulta buena devuelve exactamente el nivel de detalle que la pregunta necesita.</div>',visual:'excel'},
{id:'s3-types',unit:'s3',kind:'quiz',title:'Tipos de dato y NULL',short:'Comillas, números y ausencia',minutes:5,xp:20,skills:['WHERE'],lead:'Muchas consultas fallan por una comilla o por tratar NULL como si fuera un dato normal.',body:'<p class="explain">Texto usa <strong>comillas simples</strong>; números no. Las fechas se comparan como <code>YYYY-MM-DD</code>. <code>NULL</code> significa “valor desconocido”: no es cero ni texto vacío.</p><div class="callout warn"><code>return_date IS NULL</code> encuentra alquileres sin fecha de devolución.</div>',visual:'types',quiz:{q:'¿Cuál condición encuentra alquileres todavía sin fecha de devolución?',options:[['a','WHERE return_date = NULL'],['b','WHERE return_date IS NULL'],['c',"WHERE return_date = 'NULL'"],['d','WHERE return_date = 0']],answer:'b',feedback:{a:'NULL no se compara con =.',b:'Correcto: IS NULL pregunta por ausencia de valor.',c:'Eso busca el texto NULL.',d:'Cero no es ausencia de dato.'}}},
{id:'s3-between',unit:'s3',kind:'lab',title:'BETWEEN: rangos inclusivos',short:'Filtrar entre dos extremos',minutes:6,xp:30,skills:['WHERE'],lead:'BETWEEN expresa un rango de forma legible e incluye ambos extremos.',body:'<p class="explain"><code>length BETWEEN 60 AND 90</code> equivale a <code>length &gt;= 60 AND length &lt;= 90</code>. Películas de exactamente 60 y 90 minutos entran.</p>',visual:'between',lab:{prompt:'Muestra title y length de películas entre 60 y 90 minutos, ordenadas de menor a mayor.',starter:'SELECT title, length\nFROM film\nWHERE length BETWEEN 60 AND 90\nORDER BY length ASC;',expected:'SELECT title, length FROM film WHERE length BETWEEN 60 AND 90 ORDER BY length ASC;',required:['BETWEEN'],hints:['Filtra film.length.','BETWEEN incluye los extremos.','Ordena con ORDER BY length ASC.']}},
{id:'s3-in',unit:'s3',kind:'lab',title:'IN: un OR que se lee mejor',short:'Listas de opciones',minutes:6,xp:30,skills:['WHERE'],lead:'Cuando la pregunta dice “uno de estos valores”, IN evita cadenas largas de OR.',body:'<p class="explain">Una lista <code>IN (...)</code> hace visible que cualquiera de esos valores es válido. Los ratings son texto y conservan comillas simples.</p>',visual:'in',lab:{prompt:'Muestra title y rating para G, PG o PG-13. Ordena por rating y title.',starter:"SELECT title, rating\nFROM film\nWHERE rating IN ('G','PG','PG-13')\nORDER BY rating, title;",expected:"SELECT title, rating FROM film WHERE rating IN ('G','PG','PG-13') ORDER BY rating, title;",required:['IN'],hints:['Piensa en marcar varias casillas.','Cada rating es texto.','Usa IN con tres valores.']}},
{id:'s3-combo',unit:'s3',kind:'lab',title:'Reto: BETWEEN + IN',short:'Dos filtros, una pregunta',minutes:8,xp:45,skills:['WHERE','ORDER BY'],lead:'Combina dos ideas y comprueba una predicción con la base oficial.',body:'<p class="explain">Busca rating <strong>G o NC-17</strong> y duración entre <strong>60 y 90</strong>.</p><div class="callout"><strong>Dato de control:</strong> la presentación espera <strong>97 filas</strong>.</div>',visual:'combo',lab:{prompt:'Devuelve title, rating y length para G o NC-17, duración 60–90, de menor a mayor.',starter:"SELECT title, rating, length\nFROM film\nWHERE rating IN ('G','NC-17')\n  AND length BETWEEN 60 AND 90\nORDER BY length ASC;",expected:"SELECT title, rating, length FROM film WHERE rating IN ('G','NC-17') AND length BETWEEN 60 AND 90 ORDER BY length ASC;",required:['IN','BETWEEN'],hints:['Son dos condiciones con AND.','Una lista y un rango.','Ordena length ASC.']}},
{id:'s3-like',unit:'s3',kind:'lab',title:'LIKE: buscar patrones',short:'% y _ no son * y ?',minutes:7,xp:40,skills:['WHERE','AGG'],lead:'LIKE sirve cuando no conoces el valor exacto, pero sí cómo empieza, termina o qué contiene.',body:'<p class="explain"><code>%</code> representa cualquier secuencia y <code>_</code> exactamente un carácter. En Excel los equivalentes son <code>*</code> y <code>?</code>.</p><div class="callout warn">SQLite LIKE ignora mayúsculas por defecto; PostgreSQL distingue mayúsculas y ofrece <code>ILIKE</code>.</div>',visual:'like',lab:{prompt:'Cuenta películas cuyo título comienza por J. Nombra la columna n_peliculas.',starter:"SELECT COUNT(*) AS n_peliculas\nFROM film\nWHERE title LIKE 'J%';",expected:"SELECT COUNT(*) AS n_peliculas FROM film WHERE title LIKE 'J%';",required:['LIKE'],hints:['J debe ser el primer carácter.','Después puede venir cualquier secuencia.','El patrón es J%.']}},
{id:'s3-agg',unit:'s3',kind:'learn',title:'Funciones de agregación',short:'Muchas filas → un número',minutes:7,xp:20,skills:['AGG'],lead:'COUNT, SUM, AVG, MIN y MAX colapsan muchas filas en respuestas compactas.',body:'<p class="explain">La base real permite comprobar 1.000 películas, 16.044 alquileres, 15.861 fechas de devolución conocidas, recaudación, promedio y extremos.</p><div class="callout danger"><strong>COUNT(*) ≠ COUNT(columna).</strong> El segundo ignora NULL; por eso hay 183 alquileres sin return_date.</div>',visual:'agg'},
{id:'s3-group',unit:'s3',kind:'lab',title:'GROUP BY: repartir en baldes',short:'Cambiar el nivel de detalle',minutes:8,xp:50,skills:['GROUP BY','AGG'],lead:'Primero repartes filas por una característica; luego calculas dentro de cada balde.',body:'<p class="explain">Agrupar por <code>rating</code> convierte 1.000 películas en <strong>una fila por rating</strong>. El grupo define el nivel de detalle.</p>',visual:'group',lab:{prompt:'Cuenta películas por rating. Devuelve rating y peliculas.',starter:'SELECT rating, COUNT(*) AS peliculas\nFROM film\nGROUP BY rating\nORDER BY rating;',expected:'SELECT rating, COUNT(*) AS peliculas FROM film GROUP BY rating ORDER BY rating;',required:['GROUP BY','COUNT'],hints:['Quieres una fila por rating.','rating define los baldes.','Cuenta con COUNT(*).']}},
{id:'s3-golden',unit:'s3',kind:'quiz',title:'La regla de oro del GROUP BY',short:'Agrupada o agregada',minutes:6,xp:35,skills:['GROUP BY'],lead:'SQLite puede aceptar una consulta conceptualmente incorrecta que PostgreSQL rechazaría.',body:'<p class="explain">Cada columna del SELECT debe estar en el GROUP BY o dentro de una agregación. Pedir un <code>title</code> suelto cuando agrupas por rating no tiene una respuesta única.</p>',visual:'golden',quiz:{q:'¿Cuál consulta respeta el nivel de detalle?',options:[['a','SELECT title, rating, COUNT(*) FROM film GROUP BY rating;'],['b','SELECT COUNT(*) FROM film;'],['c','SELECT rating, COUNT(*) FROM film GROUP BY rating;'],['d','SELECT rating, COUNT(*) FROM film;']],answer:'c',feedback:{a:'title no está agrupado ni agregado.',b:'Cuenta todo, no separa por rating.',c:'Correcto: rating agrupa y COUNT resume.',d:'No forma grupos.'}}},
{id:'s3-having',unit:'s3',kind:'lab',title:'HAVING: filtrar baldes',short:'Después de calcular',minutes:7,xp:45,skills:['GROUP BY','HAVING'],lead:'WHERE filtra filas antes de agrupar; HAVING filtra grupos después del agregado.',body:'<div class="analogy"><div class="box"><b>WHERE</b><br>Filtra canicas.</div><div class="arrow">→</div><div class="box"><b>HAVING</b><br>Filtra baldes.</div></div>',visual:'having',lab:{prompt:'Devuelve rating y peliculas para clasificaciones con más de 200 películas.',starter:'SELECT rating, COUNT(*) AS peliculas\nFROM film\nGROUP BY rating\nHAVING COUNT(*) > 200\nORDER BY rating;',expected:'SELECT rating, COUNT(*) AS peliculas FROM film GROUP BY rating HAVING COUNT(*) > 200 ORDER BY rating;',required:['GROUP BY','HAVING'],hints:['El conteo no existe antes de agrupar.','WHERE no filtra un COUNT agregado.','Usa HAVING COUNT(*) > 200.']}},
{id:'s3-order',unit:'s3',kind:'learn',title:'El orden real de ejecución',short:'14.596 → 7.304 → 599 → 2',minutes:6,xp:25,skills:['WHERE','GROUP BY','HAVING'],lead:'Seguir cuántas filas quedan vivas explica por qué WHERE y HAVING son diferentes.',body:'<p class="explain">La consulta integradora parte de todos los pagos, filtra staff 2, agrupa por cliente y conserva los grupos cuyo total supera 110.</p><div class="callout">Cuando WHERE actúa, todavía no existe SUM(amount) por cliente.</div>',visual:'order'},
{id:'s3-boss',unit:'s3',kind:'lab',title:'Boss S3: filtrar → agrupar → filtrar',short:'La consulta más completa hasta S3',minutes:10,xp:80,skills:['WHERE','GROUP BY','HAVING','AGG'],lead:'Integra condición sobre filas, agrupación, suma por grupo y condición sobre el agregado.',body:'<p class="explain">Pregunta del material: <strong>¿qué clientes han gastado más de $110 con el vendedor 2?</strong></p>',visual:'order',lab:{prompt:'Devuelve customer_id y total_pagado para clientes cuyo total con staff_id=2 supera 110, de mayor a menor.',starter:'SELECT customer_id, SUM(amount) AS total_pagado\nFROM payment\nWHERE staff_id = 2\nGROUP BY customer_id\nHAVING SUM(amount) > 110\nORDER BY total_pagado DESC;',expected:'SELECT customer_id, SUM(amount) AS total_pagado FROM payment WHERE staff_id = 2 GROUP BY customer_id HAVING SUM(amount) > 110 ORDER BY total_pagado DESC;',required:['WHERE','GROUP BY','HAVING','SUM'],hints:['Filtra primero staff_id.','Agrupa por customer_id.','Filtra SUM con HAVING.']}}
];