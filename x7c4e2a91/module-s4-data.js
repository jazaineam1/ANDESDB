window.ANDESDB_S4_STEPS=[
{id:'s4-start',unit:'s4',kind:'learn',title:'La pregunta que una tabla no responde',short:'Por qué existen los JOIN',minutes:4,xp:10,skills:['JOIN'],lead:'El diseño relacional evita repetir datos, pero reparte la información. JOIN reconstruye el camino.',body:'<p class="explain">La sesión abre con <strong>“¿qué películas tienen de Nick Wahlberg?”</strong>. <code>actor</code> sabe quién es Nick; <code>film</code> sabe los títulos; ninguna tabla responde sola.</p><div class="callout"><strong>Idea guía.</strong> Antes de escribir SQL, dibuja qué tablas participan y por qué columnas se conectan.</div>',visual:'joinwhy'},
{id:'s4-keys',unit:'s4',kind:'quiz',title:'PK y FK: identificar y apuntar',short:'El código que conecta tablas',minutes:6,xp:25,skills:['JOIN'],lead:'Una PK identifica una fila; una FK guarda ese identificador en otra tabla para referirse a ella.',body:'<p class="explain">En este SQLite varias relaciones no están declaradas como restricciones: el nombre orienta, pero el motor no impide una unión equivocada. <strong>El que revisa eres tú.</strong></p><div class="analogy"><div class="box"><b>Excel</b><br>BUSCARV usa un código.</div><div class="arrow">→</div><div class="box"><b>SQL</b><br>FK apunta; JOIN cruza.</div></div>',visual:'keys',quiz:{q:'customer.address_id debe conectarse con…',options:[['a','address.customer_id'],['b','address.address_id'],['c','customer.customer_id'],['d','rental.address_id']],answer:'b',feedback:{a:'address no necesita customer_id.',b:'Correcto: address_id apunta al identificador de address.',c:'Son entidades distintas.',d:'rental no describe direcciones.'}}},
{id:'s4-card',unit:'s4',kind:'learn',title:'Cardinalidad: cuántas filas pueden aparecer',short:'1:1 · 1:N · N:N',minutes:7,xp:25,skills:['JOIN'],lead:'La cardinalidad anticipa qué puede ocurrir con el número de filas de una unión.',body:'<p class="explain"><strong>1:1</strong>: una pareja. <strong>1:N</strong>: el lado 1 puede repetirse. <strong>N:N</strong>: hace falta una tabla puente.</p><div class="callout warn">En la base real, 1.000 películas producen <strong>4.581 filas</strong> al unir <code>film</code> con <code>inventory</code>.</div>',visual:'cardinality'},
{id:'s4-union',unit:'s4',kind:'quiz',title:'UNION vs UNION ALL',short:'Apilar no es enganchar',minutes:6,xp:30,skills:['UNION'],lead:'UNION apila y elimina duplicados; UNION ALL conserva cada fila.',body:'<p class="explain">Al apilar hechos normalmente no quieres que dos filas idénticas desaparezcan por coincidencia.</p><div class="callout danger"><strong>Trampa:</strong> UNION alinea por posición, no por nombre. Un orden de columnas incorrecto puede no dar error.</div>',visual:'union',quiz:{q:'Quieres conservar 200 actores + 599 clientes aunque se repita un nombre. ¿Qué usas?',options:[['a','UNION'],['b','UNION ALL'],['c','INNER JOIN'],['d','LEFT JOIN']],answer:'b',feedback:{a:'UNION puede borrar filas.',b:'Correcto: UNION ALL conserva las 799.',c:'JOIN no apila.',d:'LEFT JOIN no apila.'}}},
{id:'s4-unionlab',unit:'s4',kind:'lab',title:'Reto: una lista de personas',short:'Clientes + empleados',minutes:8,xp:50,skills:['UNION'],lead:'El desafío de la presentación pide una sola lista de clientes y empleados con su tipo.',body:'<p class="explain">Hay <strong>599 clientes</strong> y <strong>2 empleados</strong>: deben salir 601 filas. Una columna fija hace explícita la procedencia.</p>',visual:'union',lab:{prompt:'Apila customer y staff. Devuelve first_name, last_name y tipo; conserva todo y ordena por apellido y nombre.',starter:"SELECT first_name, last_name, 'cliente' AS tipo\nFROM customer\nUNION ALL\nSELECT first_name, last_name, 'empleado' AS tipo\nFROM staff\nORDER BY last_name, first_name;",expected:"SELECT first_name, last_name, 'cliente' AS tipo FROM customer UNION ALL SELECT first_name, last_name, 'empleado' AS tipo FROM staff ORDER BY last_name, first_name;",required:['UNION ALL'],hints:['Dos SELECT con igual cantidad de columnas.','La tercera columna puede ser texto fijo AS tipo.','Conserva hechos con UNION ALL.']}},
{id:'s4-five',unit:'s4',kind:'learn',title:'Las cinco preguntas del JOIN',short:'Diseña antes de escribir',minutes:7,xp:25,skills:['JOIN'],lead:'Convierte un JOIN largo en cinco decisiones repetibles.',body:'<p class="explain">Decide: <strong>1)</strong> tabla izquierda, <strong>2)</strong> tabla derecha, <strong>3)</strong> columnas de salida, <strong>4)</strong> condición <code>ON</code>, <strong>5)</strong> qué hacer con filas sin pareja.</p><div class="callout">Si te atascas escribiendo, probablemente una de esas decisiones aún no está resuelta.</div>',visual:'five'},
{id:'s4-inner',unit:'s4',kind:'lab',title:'INNER JOIN: solo parejas',short:'film ↔ language',minutes:8,xp:55,skills:['JOIN'],lead:'INNER JOIN conserva solamente filas con pareja y no anuncia cuáles descartó.',body:'<p class="explain"><code>film.language_id</code> apunta a <code>language.language_id</code>.</p><div class="callout warn">Si un filtro devuelve cero, prueba también un valor que sí debería existir: así auditas el JOIN antes de concluir.</div>',visual:'inner',lab:{prompt:'Muestra film_id, title y nombre del idioma de cada película. Ordena por film_id.',starter:'SELECT f.film_id, f.title, l.name AS idioma\nFROM film f\nJOIN language l\n  ON f.language_id = l.language_id\nORDER BY f.film_id;',expected:'SELECT f.film_id, f.title, l.name AS idioma FROM film f JOIN language l ON f.language_id = l.language_id ORDER BY f.film_id;',required:['JOIN',' ON '],hints:['film es izquierda; language derecha.','La llave es language_id.','Usa alias para columnas.']}},
{id:'s4-silent',unit:'s4',kind:'quiz',title:'El JOIN puede borrar o multiplicar filas',short:'Auditar el resultado',minutes:7,xp:35,skills:['JOIN'],lead:'La cantidad de filas depende de la cardinalidad y de las coincidencias.',body:'<p class="explain">INNER descarta filas sin pareja; en 1:N una fila del lado 1 puede repetirse. Ambas cosas son normales: el riesgo es no preverlas.</p>',visual:'cardinality',quiz:{q:'film tiene 1.000 filas y film JOIN inventory produce 4.581. ¿Por qué?',options:[['a','JOIN duplicó por error'],['b','Una película puede tener varias copias'],['c','JOIN siempre multiplica 4,581'],['d','film_id dejó de ser PK']],answer:'b',feedback:{a:'La cardinalidad explica la repetición.',b:'Correcto: una película aparece por cada copia.',c:'No hay factor fijo.',d:'La PK sigue siendo única en film.'}}},
{id:'s4-left',unit:'s4',kind:'lab',title:'LEFT JOIN: conservar aunque no haya pareja',short:'Buscar ausencias',minutes:9,xp:60,skills:['JOIN','WHERE'],lead:'LEFT conserva la tabla izquierda y usa NULL cuando no encuentra pareja.',body:'<p class="explain">Esto permite convertir una ausencia en una pregunta consultable: <strong>películas sin ninguna copia en inventory</strong>.</p>',visual:'left',lab:{prompt:'Encuentra film_id y title de películas sin filas en inventory. Ordena por film_id.',starter:'SELECT f.film_id, f.title\nFROM film f\nLEFT JOIN inventory i\n  ON f.film_id = i.film_id\nWHERE i.inventory_id IS NULL\nORDER BY f.film_id;',expected:'SELECT f.film_id, f.title FROM film f LEFT JOIN inventory i ON f.film_id = i.film_id WHERE i.inventory_id IS NULL ORDER BY f.film_id;',required:['LEFT JOIN','IS NULL'],hints:['Conserva film completa.','Sin coincidencia inventory queda NULL.','Filtra i.inventory_id IS NULL.']}},
{id:'s4-bridge',unit:'s4',kind:'lab',title:'N:N: cruzar por una tabla puente',short:'Nick → film_actor → film',minutes:10,xp:75,skills:['JOIN'],lead:'Actor y película son muchos-a-muchos: film_actor guarda cada pareja.',body:'<p class="explain">Ya puedes resolver la pregunta que abrió S4. El camino tiene dos saltos: <code>actor → film_actor → film</code>.</p>',visual:'bridge',lab:{prompt:'Muestra film_id y title de películas de Nick Wahlberg, ordenadas por title.',starter:"SELECT f.film_id, f.title\nFROM actor a\nJOIN film_actor fa ON a.actor_id = fa.actor_id\nJOIN film f ON fa.film_id = f.film_id\nWHERE UPPER(a.first_name) = 'NICK'\n  AND UPPER(a.last_name) = 'WAHLBERG'\nORDER BY f.title;",expected:"SELECT f.film_id, f.title FROM actor a JOIN film_actor fa ON a.actor_id = fa.actor_id JOIN film f ON fa.film_id = f.film_id WHERE UPPER(a.first_name)='NICK' AND UPPER(a.last_name)='WAHLBERG' ORDER BY f.title;",required:['JOIN','FILM_ACTOR'],hints:['No hay actor → film directo.','film_actor tiene ambas llaves.','Necesitas dos JOIN.']}},
{id:'s4-chain',unit:'s4',kind:'lab',title:'Encadenar JOIN: seguir el recorrido',short:'customer → rental → inventory → film',minutes:11,xp:85,skills:['JOIN'],lead:'Cada JOIN representa un salto del mapa relacional.',body:'<p class="explain">Eleanor Hunt (customer_id 148) tiene 46 alquileres. Para llegar al título recorres <code>customer → rental → inventory → film</code>.</p><div class="callout">El resultado debe seguir teniendo un registro por alquiler.</div>',visual:'chain',lab:{prompt:'Para customer_id 148, muestra rental_id y title de cada alquiler. Ordena por rental_id.',starter:'SELECT r.rental_id, f.title\nFROM customer c\nJOIN rental r ON c.customer_id = r.customer_id\nJOIN inventory i ON r.inventory_id = i.inventory_id\nJOIN film f ON i.film_id = f.film_id\nWHERE c.customer_id = 148\nORDER BY r.rental_id;',expected:'SELECT r.rental_id, f.title FROM customer c JOIN rental r ON c.customer_id = r.customer_id JOIN inventory i ON r.inventory_id = i.inventory_id JOIN film f ON i.film_id = f.film_id WHERE c.customer_id = 148 ORDER BY r.rental_id;',required:['JOIN','INVENTORY'],hints:['No saltes customer → film.','rental guarda inventory_id.','inventory guarda film_id.']}},
{id:'s4-multiply',unit:'s4',kind:'quiz',title:'Contar después de un JOIN',short:'Filas ≠ entidades',minutes:7,xp:35,skills:['JOIN','AGG'],lead:'Después de 1:N, contar filas puede inflar el número de entidades.',body:'<p class="explain">Si un rental aparece en varios payment, <code>COUNT(*)</code> cuenta filas del JOIN. Para alquileres únicos piensa en <code>COUNT(DISTINCT r.rental_id)</code>.</p>',visual:'multiply',quiz:{q:'Quieres contar alquileres únicos después de rental JOIN payment. ¿Qué expresión es más segura?',options:[['a','COUNT(*)'],['b','COUNT(p.payment_id)'],['c','COUNT(DISTINCT r.rental_id)'],['d','SUM(r.rental_id)']],answer:'c',feedback:{a:'Cuenta filas del JOIN.',b:'Cuenta pagos.',c:'Correcto: protege la entidad alquiler.',d:'Sumar IDs no cuenta alquileres.'}}},
{id:'s4-boss',unit:'s4',kind:'lab',title:'Boss S4: el cliente con más alquileres',short:'JOIN + GROUP BY + DISTINCT',minutes:12,xp:100,skills:['JOIN','GROUP BY','AGG'],lead:'Une S3 y S4: relación correcta, nivel correcto, agregación y ranking.',body:'<p class="explain">La presentación identifica a <strong>Eleanor Hunt</strong> con <strong>46 alquileres</strong>. Debes producir esa evidencia.</p>',visual:'boss',lab:{prompt:'Encuentra el cliente con más alquileres. Devuelve customer_id, first_name, last_name y alquileres usando COUNT(DISTINCT rental_id).',starter:'SELECT c.customer_id, c.first_name, c.last_name,\n       COUNT(DISTINCT r.rental_id) AS alquileres\nFROM customer c\nJOIN rental r ON c.customer_id = r.customer_id\nGROUP BY c.customer_id, c.first_name, c.last_name\nORDER BY alquileres DESC, c.customer_id\nLIMIT 1;',expected:'SELECT c.customer_id, c.first_name, c.last_name, COUNT(DISTINCT r.rental_id) AS alquileres FROM customer c JOIN rental r ON c.customer_id = r.customer_id GROUP BY c.customer_id, c.first_name, c.last_name ORDER BY alquileres DESC, c.customer_id LIMIT 1;',required:['JOIN','GROUP BY','COUNT','DISTINCT'],hints:['Empieza customer + rental.','Una fila final por cliente.','Ordena conteo DESC y LIMIT 1.']}}
];